<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Job</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background: #ffffff;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .btn {
            transition: background-color 0.3s ease;
        }
        .btn:hover {
            background-color: #0056b3;
        }
        .btn-secondary {
            transition: background-color 0.3s ease;
        }
        .btn-secondary:hover {
            background-color: #c82333;
        }
    </style>
</head>
<body class="bg-gray-100">
    <div class="container">
        <header class="flex justify-between items-center mb-6">
            <a href="/user-profile" class="text-blue-500 hover:text-blue-700">Back to Profile</a>
            <div class="space-x-2">
                <a href="/job/edit/<%= job._id %>" class="btn bg-blue-500 text-white py-2 px-4 rounded-lg shadow-lg hover:bg-blue-600">Edit Job</a>
                <form action="/job/delete/<%= job._id %>" method="POST" class="inline" onsubmit="return confirm('Are you sure you want to delete this job?');">
                    <button type="submit" class="btn-secondary bg-red-500 text-white py-2 px-4 rounded-lg shadow-lg hover:bg-red-600">Delete Job</button>
                </form>
            </div>
        </header>
        <main>
            <h1 class="text-3xl font-bold mb-4"><%= job.title %></h1>
            <p class="text-lg mb-4"><strong>Description:</strong> <%= job.description %></p>
            <p class="text-lg mb-4"><strong>Location:</strong> <%= job.location %></p>
            <p class="text-lg mb-4"><strong>Salary:</strong> <%= job.salary %></p>
            <p class="text-lg mb-4"><strong>Job Type:</strong> <%= job.jobType %></p>
            <p class="text-lg mb-4"><strong>Company:</strong> <%= job.company %></p>
            <% if (job.companyLogo) { %>
                <div class="mb-4">
                    <img src="<%= job.companyLogo %>" alt="Company Logo" class="w-32 h-32 object-cover rounded-full">
                </div>
            <% } %>
            <p class="text-lg mb-4"><strong>Application Deadline:</strong> <%= job.applicationDeadline.toDateString() %></p>
            <p class="text-lg mb-4"><strong>Posted by:</strong> <%= job.user.username %></p>
        </main>
    </div>
</body>
</html>
