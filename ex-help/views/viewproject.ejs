<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title><%= project.title %></title>
    <style>
        .pdf-viewer {
            width: 100%;
            height: 600px; /* Adjust height as needed */
            border: none;
        }
    </style>
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center">
    <div class="w-full max-w-4xl bg-white p-8 rounded-lg shadow-lg">
        <!-- Header with Action Buttons -->
        <header class="flex justify-between items-center mb-6">
            <a href="/user-profile" class="text-blue-500 hover:text-blue-700 text-lg">Back to Profile</a>
            <a href="/" class="bg-gradient-to-r from-blue-400 to-blue-600 text-white py-2 px-4 rounded-lg hover:bg-blue-700 shadow-lg flex items-center ml-80 btn mb-2 lg:mb-0">
                <i class="ri-home-line mr-2"></i> Home
            </a>
            <div class="space-x-2">
                
                <a href="/project/edit/<%= project._id %>" class="bg-blue-500 text-white py-2 px-4 rounded-lg shadow-lg hover:bg-blue-600">Edit Project</a>
                <form action="/project/delete/<%= project._id %>" method="POST" class="inline" onsubmit="return confirm('Are you sure you want to delete this project?');">
                    <button type="submit" class="bg-red-500 text-white py-2 px-4 rounded-lg shadow-lg hover:bg-red-600">Delete Project</button>
                </form>
            </div>
        </header>
        
        <!-- Project Title -->
        <h1 class="text-4xl font-bold mb-4 text-indigo-900 text-center"><%= project.title %></h1>
        
        <!-- Project Description -->
        <p class="mb-6 text-lg text-gray-700 text-center"><%= project.description %></p>

        <!-- View Project PDF -->
        <% if (project.pdf) { %>
            <div class="text-center mb-6">
                <iframe class="pdf-viewer" src="data:application/pdf;base64,<%= project.pdf %>"></iframe>
            </div>
        <% } else { %>
            <p class="text-center text-gray-600">No PDF available for this project.</p>
        <% } %>
    </div>
</body>
</html>
